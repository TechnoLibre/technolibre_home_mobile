# Démo Tornado

Un serveur asynchrone minimal : une route JSON, une route WebSocket, et un
gestionnaire d'erreur. Sert à voir la coloration Python et les décorateurs.

```bash
python -m pip install tornado
python app.py   # http://localhost:8888/api/status
```
